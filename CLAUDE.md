# CLAUDE.md — 소상공인 상권 AI 입지분석 서비스

> 이 파일은 프로젝트 루트에 두세요. Claude Code가 매 세션 자동으로 읽습니다.

## 프로젝트 목표
- 대전지역 공공데이터 활용 공모전(웹/앱 개발 분야) 출품작.
- **소상공인시장진흥공단 상가(상권) 공공데이터**를 활용해, 예비창업자가
  자연어로 물으면 AI가 입지를 해석·추천해주는 웹 서비스를 만든다.
- 심사 핵심은 두 가지: (1) 공공데이터가 서비스의 **핵심 기능을 실제로 구동**하는가,
  (2) 심사 시점에 URL이 **살아서 끝까지 동작**하는가.
- 기존 상권분석 대시보드(공단 sg.sbiz.or.kr 등)와의 차별점 = **자연어 대화형 해석·추천**.
  단순 지표 나열 대시보드를 또 만드는 게 아니다.

## 핵심 기능 (MVP를 먼저)
사용자 흐름: 자연어 질의 입력 → 백엔드가 지역·업종 파싱 → 상권 API 조회+집계
→ GPT-4o-mini가 경쟁 밀집도·공백 업종·리스크를 종합한 **서사형 리포트** 생성
→ 지도 + 리포트로 표시.

- 타깃/메인 훅: **대전·세종 예비창업자 대상, 자연어 상담형 입지 분석.**
  사용자가 말로 물으면 AI가 상권 데이터를 해석해 "여기서 이 업종은 이래서 유망/비추천"까지
  대화형으로 답해준다. 메인 경험은 지표 대시보드가 아니라 **AI 창업 상담사**다.
  단, 답변은 반드시 조회한 상권 데이터의 실제 수치에 근거해야 한다(일반론 GPT 답변 금지).

## 기술 스택
- Frontend: React (배포: Vercel)
- Backend: Spring Boot 4.1 / Java 21, REST API
- DB: MySQL 8 (Docker 컨테이너)
- AI: OpenAI **gpt-4o-mini** (호출은 백엔드에서만)
- 외부 데이터: 소상공인시장진흥공단 상가(상권)정보 오픈API (data.go.kr)
- 배포: Vercel(프론트) + Oracle Cloud Always Free VM(백엔드, **ARM aarch64**) + Cloudflare Tunnel(HTTPS)

## 아키텍처 규칙 (반드시 준수)
- **모든 외부 API 키(OpenAI, data.go.kr)는 백엔드에만.** 프론트 번들/env에 절대 노출 금지.
- 호출 흐름은 항상 React → Spring Boot → (상권 API / OpenAI).
  프론트에서 외부 API를 직접 부르지 않는다.
- 키·시크릿은 환경변수로 주입, git 커밋 금지(.gitignore + .env.example 제공).
- 백엔드는 HTTPS 필수(Cloudflare Tunnel). CORS는 Vercel 도메인을 허용 오리진으로.
- 배포 대상이 ARM(aarch64)이므로 Docker 이미지·의존성의 arm64 호환을 확인.
- 설정(포트, CORS 오리진, DB 접속, 키)은 하드코딩하지 말고 전부 환경변수로 외부화.

## 데이터 명세 (소상공인 상권 API)
- data.go.kr 오픈API, 인증키 필요. 반경·행정동·사각형 단위 상가 조회 지원.
- 주요 필드: 상호명, 상권업종명(대/중/소분류), 지번·도로명주소, 경도, 위도.
- **개발계정은 하루 호출 한도**가 있으니, 반복 조회 데이터는 DB에 캐싱해서 재사용.
- 실제 응답 샘플을 먼저 받아 스키마를 확인하고 파싱 유틸부터 만든다.
  (응답 구조를 추측으로 코딩하지 말 것 — 실제 JSON을 보고 모델링)

## 개발 원칙
- **작동하는 최소 버전(E2E: 질의→조회→리포트)을 가장 먼저 배포**하고 이후 개선.
- 작은 단위로 커밋하고 각 단계 실행을 확인.
- "기관 데이터가 활용된 부분"을 코드 구조와 README에 명확히 남긴다(심사 항목).
- 완벽함보다 "돌아가고 데이터 활용이 분명한" 상태를 우선.

## 하지 말 것
- 프론트에 API 키 노출.
- 상권 데이터를 단순 조회·표시만 하고 끝내기(데이터가 핵심 기능을 구동해야 함).
- 조기 과설계(불필요한 추상화·마이크로서비스화 등).

## 개발 환경
- macOS, Docker, MySQL 사용 경험 있음. Claude Code CLI로 작업.

## 일정 (고정)
- 접수 마감: 8/31(월) — sung1020@hanbat.ac.kr 이메일 제출.
- 데이터 기준일: 2025.11.15 ~ 2026.8.31.
- 서비스 URL은 9월 발표(셋째주)까지 상시 가동 유지.
